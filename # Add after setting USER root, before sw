FROM ubuntu:22.04

# Add after setting USER root, before switching to appuser
RUN apt-get update && \
    apt-get install -y nmap metasploit-framework sqlmap recon-ng gobuster hydra john hashcat wpscan arachni && \
    apt-get clean && rm -rf /var/lib/apt/lists/*